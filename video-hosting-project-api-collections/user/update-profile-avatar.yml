info:
  name: update-profile-avatar
  type: http
  seq: 9

http:
  method: PATCH
  url: "{{server}}/users/update-profile-avatar"
  body:
    type: multipart-form
    data:
      - name: avatar
        type: file
        value:
          - /home/tuban/Downloads/8880080797076757786.jpg
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
