info:
  name: sign-out
  type: http
  seq: 3

http:
  method: POST
  url: "{{server}}/users/sign-out"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
