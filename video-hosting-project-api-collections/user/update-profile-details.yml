info:
  name: update-profile-details
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{server}}/users/update-profile-details"
  body:
    type: json
    data: |-
      {
        "fullName": "S. Sarma",
        "username": "ssarma24",
        "email": "ss24@gmail.com",
        "gender": "F"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
