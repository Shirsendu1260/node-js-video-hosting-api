info:
  name: get-auth-user
  type: http
  seq: 5

http:
  method: GET
  url: "{{server}}/users/get-auth-user"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
