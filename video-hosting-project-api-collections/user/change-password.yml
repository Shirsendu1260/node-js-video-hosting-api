info:
  name: change-password
  type: http
  seq: 7

http:
  method: PATCH
  url: "{{server}}/users/change-password"
  body:
    type: json
    data: |-
      {
        "oldPassword": "",
        "newPassword": "",
        "confirmedPassword": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
