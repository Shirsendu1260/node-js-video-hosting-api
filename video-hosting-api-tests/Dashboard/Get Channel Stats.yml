info:
  name: Get Channel Stats
  type: http
  seq: 1

http:
  method: GET
  url: "{{server}}/dashboard/{{username}}/stats"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
