info:
  name: Remove Video from Playlist
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{server}}/playlist/remove/{{base64PlaylistId}}/{{base64VideoId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
