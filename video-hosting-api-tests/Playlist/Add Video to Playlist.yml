info:
  name: Add Video to Playlist
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{server}}/playlist/add/{{base64PlaylistId}}/{{base64VideoId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
