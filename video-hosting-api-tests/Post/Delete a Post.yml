info:
  name: Delete a Post
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{server}}/post/{{base64PostId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
