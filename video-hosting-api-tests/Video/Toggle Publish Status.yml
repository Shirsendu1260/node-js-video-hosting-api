info:
  name: Toggle Publish Status
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{server}}/video/{{base64VideoId}}/publish-status"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
