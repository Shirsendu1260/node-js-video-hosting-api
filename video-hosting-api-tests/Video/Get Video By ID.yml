info:
  name: Get Video By ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{server}}/video/{{base64VideoId}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
