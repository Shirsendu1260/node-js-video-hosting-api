info:
  name: Delete Video
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{server}}/video/{{base64VideoId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
