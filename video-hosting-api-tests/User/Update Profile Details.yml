info:
  name: Update Profile Details
  type: http
  seq: 1

http:
  method: PATCH
  url: "{{server}}/user/update-profile-details"
  body:
    type: json
    data: |-
      {
        "fullName": "TEST",
        "username": "test1",
        "email": "test1@gmail.com",
        "gender": "M"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
