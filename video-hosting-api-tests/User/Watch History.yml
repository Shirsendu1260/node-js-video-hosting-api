info:
  name: Watch History
  type: http
  seq: 5

http:
  method: GET
  url: "{{server}}/user/watch-history"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
