info:
  name: Get Auth User
  type: http
  seq: 1

http:
  method: GET
  url: "{{server}}/user/get-auth-user"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
