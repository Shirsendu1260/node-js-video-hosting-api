info:
  name: Update a Comment
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{server}}/comment/{{base64ParentCommentId}}"
  body:
    type: json
    data: |-
      {
          "content": "Perfect color contrast, it looks great."
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
