info:
  name: Add Reply to a Comment of a Post
  type: http
  seq: 10

http:
  method: POST
  url: "{{server}}/comment/p/{{base64PostId}}/reply/{{base64ParentCommentId}}/add"
  body:
    type: json
    data: |-
      {
          "content": "Best."
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
