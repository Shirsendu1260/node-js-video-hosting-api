info:
  name: Get Replies of a Comment of a Video
  type: http
  seq: 4

http:
  method: GET
  url: "{{server}}/comment/v/{{base64VideoId}}/reply/{{base64ParentCommentId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
