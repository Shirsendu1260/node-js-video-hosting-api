info:
  name: Delete a Comment
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{server}}/comment/{{base64CommentId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
