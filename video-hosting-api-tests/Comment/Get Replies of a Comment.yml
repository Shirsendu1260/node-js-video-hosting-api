info:
  name: Get Replies of a Comment
  type: http
  seq: 4

http:
  method: GET
  url: "{{server}}/comment/{{base64VideoId}}/{{base64ParentCommentId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
