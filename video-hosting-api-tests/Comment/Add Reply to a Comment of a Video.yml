info:
  name: Add Reply to a Comment of a Video
  type: http
  seq: 3

http:
  method: POST
  url: "{{server}}/comment/v/{{base64VideoId}}/reply/{{base64ParentCommentId}}/add"
  body:
    type: json
    data: |-
      {
          "content": "Nature is beautiful."
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
